import {COLOR_GRAY} from '../../styles/common';

export const chartTheme = {
  axis: {
    style: {
      axis: {
        stroke: COLOR_GRAY,
      },
      tickLabels: {
        fill: COLOR_GRAY,
      },
      grid: {
        stroke: COLOR_GRAY,
        opacity: 0.1,
      },
    },
  },
};
